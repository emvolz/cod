# Computes the CH index for selecting clustering thresholds

Computes the CH index for selecting clustering thresholds

## Usage

``` r
chindex(clusterdf)
```

## Arguments

- clusterdf:

  Output from \`computeclusters\`
